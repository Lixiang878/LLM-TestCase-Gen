# Core has NO required dependencies (Python standard library only).
# Optional providers are lazy-imported; install only what you use:

# openai>=1.0        # for the OpenAIProvider (OpenAI / Wenxin / Qwen / vLLM)
# requests>=2.28     # fallback HTTP client for OpenAIProvider

# Development / tests:
# pytest>=7.0
# ruff>=0.4
